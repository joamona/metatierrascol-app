import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'metatierrascol.app.com',
  appName: 'MetaTierras Colombia',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
